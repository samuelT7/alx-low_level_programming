#include "main.h"
/**
 * _isdigit - a function that checks for a digit (0 through 9)
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 for others
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
