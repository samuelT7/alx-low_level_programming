#include <stdio.h>
/**
 * main - A C program that prints the alphabetic
 * Return: Always (Success)
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	putchar(a);
	putchar('\n');
	return (0);
}
